import { EventLockerDirective } from './event-locker.directive';

describe('EventLockerDirective', () => {
  it('should create an instance', () => {
    const directive = new EventLockerDirective();
    expect(directive).toBeTruthy();
  });
});
